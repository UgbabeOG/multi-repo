# Project Management (React + Node)

This folder contains two subprojects: `frontend` (React) and `backend` (Node/Express).

Run backend:

```
cd project-management/backend
npm install
node server.js
```

Run frontend (create-react-app or similar):

```
cd project-management/frontend
npm install
npm start
```
